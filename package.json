{
  "name": "dialogue-export",
  "version": "0.5.1",
  "private": true,
  "description": "Local conversation export for Firefox \u2014 development checks",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "@sinonjs/fake-timers": "15.4.0",
    "jsdom": "26.1.0"
  }
}
